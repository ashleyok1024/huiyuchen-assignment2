import { ToastOption } from './type';
declare class EventEmiter {
    private queue;
    private trigger;
    constructor();
    add(option: ToastOption): void;
    emit(option: ToastOption): void;
    remove(toastId: string | number): void;
    on(callback: Function): void;
}
declare const _default: EventEmiter;
export default _default;
