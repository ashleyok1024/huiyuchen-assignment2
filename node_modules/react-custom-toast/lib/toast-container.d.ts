import React from 'react';
import { Placement, ToastOption } from './type';
import './style.styl';
interface Props {
    component?: React.FunctionComponent<any>;
    clickClose?: boolean;
}
interface State {
    placement: Placement;
    toasts: any[];
}
declare type PlacementToasts = {
    [index in Placement]?: ToastOption[];
};
export default class ToastContainer extends React.Component<Props, State> {
    state: Readonly<State>;
    componentDidMount(): void;
    render(): JSX.Element;
    getPlacementToasts(): PlacementToasts;
    renderPlacement(): JSX.Element[];
    renderToasts(toasts: ToastOption[], placement: Placement): JSX.Element[];
    handleClick: (toast: ToastOption) => () => void;
    getAnimateClass(toast: ToastOption): any;
}
export {};
